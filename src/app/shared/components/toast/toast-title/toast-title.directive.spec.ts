import { ToastTitleDirective } from './toast-title.directive';

describe('ToastTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastTitleDirective();
    expect(directive).toBeTruthy();
  });
});
