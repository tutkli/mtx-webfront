import { ToastDescriptionDirective } from './toast-description.directive';

describe('ToastDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
